# Stage 1 — Build / install

Show it installs cleanly on a fresh machine. Capture:

- [ ] `node --version` (≥ 18).
- [ ] The tail of `npm install` showing `added NNN packages` and **no** `npm error`.
- [ ] `npm run typecheck` output (no errors).
- [ ] `npm run verify` output (the offline static check — should print **PASS**).

Save terminal screenshots or text logs here (e.g. `install.txt`, `typecheck.txt`,
`verify.txt`).
